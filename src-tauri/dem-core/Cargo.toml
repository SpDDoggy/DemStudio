[package]
name = "dem-core"
version = "0.1.0"
description = "Platform-independent DEM parsing and normalization core for DEM Studio"
edition = "2021"
rust-version = "1.77.2"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tiff = "0.11.3"
