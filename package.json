{
  "name": "dem-studio",
  "private": true,
  "version": "0.12.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:web": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:build:windows": "tauri build --bundles nsis",
    "desktop:build:smoke:windows": "tauri build --debug --no-bundle --config src-tauri/tauri.smoke.conf.json",
    "verify": "node scripts/verify-baseline.mjs",
    "verify:runtime:windows": "npm run desktop:build:smoke:windows && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/runtime-smoke.ps1",
    "migrate:lens": "node scripts/migrate-lens-state.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-store": "^2.0.0",
    "geotiff": "2.1.3",
    "three": "0.164.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@9.8.1"
}
